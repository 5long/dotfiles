[user]
  name = Whyme Lyu
  email = callme5long@gmail.com
[alias]
  aa = add --all .
  amend = commit --amend -C HEAD
  b = branch -vv
  br = branch
  ca = commit --all --verbose
  cb = checkout HEAD -b
  ci = commit
  cln = clean -Xd
  co = checkout
  cob = checkout origin/master -b
  cub = checkout upstream/master -b
  dc = diff --cached
  di = diff
  du = diff @{u}
  dus = diff @{u} --stat
  fa = fetch --all
  ff = merge --ff-only
  h = help
  ic = commit -vp
  l = log --graph --pretty=grb
  ll = log --graph --pretty=grb --stat
  lu = log --graph --pretty=grb @{u}..
  lus = log --graph --pretty=grb @{u}.. --stat
  ph = push
  pl = pull
  po = push origin
  rb = rebase
  rs = reset
  rv = checkout HEAD --force --
  s = status -bs
  sh = stash
  sl = stash list
  sp = stash show
  st = status -bs
  sv = stash save -u
  tip = log --stat -1
[branch]
  autosetupmerge = always
[pretty]
  grb = %C(yellow)%h%Creset %Cgreen(%ai)%Creset %C(bold)<%an>%Creset %C(bold green)%d%Creset %s
[core]
  pager = less -+FSRX -FRX
[push]
  default = current
[pull]
  rebase = true
[rebase]
  autoStash = true
  autoSquash = true
[merge]
  defaultToUpstream = true
  tool = vimdiff
[diff]
  tool = vimdiff
  algorithm = histogram
  mnemonicprefix = true
  indentHeuristic = true
  compactionHeuristic = true
[stash]
  showPatch = true
[log]
  decorate = true
[credential]
  helper = cache --timeout=36000
[rerere]
  enabled = true
[commit]
  verbose = true
